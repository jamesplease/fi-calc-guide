---
permalink: search.json
eleventyExcludeFromCollections: true
---
[
{%- for page in collections.all | searchable -%}
  {%- if page.data.layout %}
  {
    "url": "{{ page.url }}",
    "title": "{{ page.data.title | safe }}",
    "summary": "{{ page.templateContent | summary | safe }}",
    "index": "{{ page.templateContent | squash }}"
  }{% if not loop.last %},{% endif %}
  {%- endif -%}
{%- endfor -%}
]